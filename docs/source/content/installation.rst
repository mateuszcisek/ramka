Installation
============

To install the package using ``pip``, run the following command:

.. code-block:: bash

   pip install ramka

To add it as a ``poetry`` dependency, run:

.. code-block:: bash

   poetry add ramka
